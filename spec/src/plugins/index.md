# Plugins

Lapis groups every bundled plugin beneath one canonical `Plugins / <Plugin>`
specification hierarchy. Plugin-owned panels are nested beneath that plugin's
`Panels` chapter, while cross-plugin lifecycle remains in Plugin Model.
