# Storybook Catalog

Storybook Vite aliases resolve first-party plugin sources, including
`@lapis-notes/spellcheck`, through the same catalog host as Word Count.
The editor-demo catalog gives Reading outline geometry its own focused,
`visual-pending` story so viewport placement and scroll behavior do not depend
on the broader Markdown Authoring interaction sequence.

## Requirements

| ID         | Requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LN-CAT-001 | The repository Storybook catalog MUST run on port 7010 as the workspace docs host. Authorized plugin packages MAY host an additional Storybook on a dedicated port for package-local UI and MUST NOT replace the 7010 catalog.                                                                                                                                                                                                                                                                                                                                                                                            |
| LN-CAT-002 | Every `@lapis-notes/ui` family consumed by `@lapis-notes/api` (direct imports; excluding type-only surfaces such as sheet `Side`) MUST have an `API/<Name>` Storybook story under `stories/api/` with a play that exercises meaningful behavior.                                                                                                                                                                                                                                                                                                                                                                          |
| LN-CAT-003 | Catalog metadata under `stories/catalog/` MUST list API UI verification ids, governing spec links, public surfaces, and story ids.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| LN-CAT-004 | Story and MDX docs MUST link to canonical `spec/src` chapters instead of copying normative prose.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| LN-CAT-005 | Shared shadcn families destined for design-core SHOULD NOT duplicate full design-core Storybook coverage; document status in `MIGRATION.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| LN-CAT-006 | Visual stories under `API/` MUST ship Visual Delta PNG baselines (`baselinePathMode: nested-import`) tagged `visual-pending` until human review promotes them to `visual-approved`. Stories tagged `skip-visual` are exempt.                                                                                                                                                                                                                                                                                                                                                                                              |
| LN-CAT-007 | Interactive plays MUST assert outcomes via a demo status surface (`data-testid="api-ui-status"`) and use `storybook/test` helpers; portaled overlays MUST assert against `canvasElement.ownerDocument.body`.                                                                                                                                                                                                                                                                                                                                                                                                              |
| LN-CAT-008 | Storybook accessibility checks MUST fail `pnpm test:storybook` on axe violations: `.storybook/vitest.setup.ts` registers `@storybook/addon-a11y/preview`, and preview sets `parameters.a11y.test: "error"`.                                                                                                                                                                                                                                                                                                                                                                                                               |
| LN-CAT-009 | Storybook MUST load design-core `styles.css` + Lapis theme as style authority, import `@lapis-notes/ui/theme.css` (Obsidian alias tokens only), expose an Obsidian/Default brand selector with Obsidian initially selected, and expose design-core's independent light/dark colour-mode toggle. Host `@tailwindcss/vite` MAY remain for story/demo layout only — not for component paint.                                                                                                                                                                                                                                 |
| LN-CAT-010 | Story / demo Svelte under `stories/` MAY use host Tailwind for layout; that usage MUST remain excluded from `pnpm check:no-tailwind`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| LN-CAT-011 | `Workspace/Shell` MUST document the `@lapis-notes/workspace` integration with persisted desktop and mobile stories linked to `workspace-shell.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| LN-CAT-012 | Workspace shell plays MUST boot a real api `App`, exercise meaningful controller-backed shell behavior, and verify the story adapter receives the expected workspace persistence update.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| LN-CAT-013 | Workspace shell stories MUST use design-core's shared full-viewport catalog layout, including an unconstrained mobile canvas, and MUST NOT load Lapis/community plugins. They MUST include the minimal design-core notifications chrome plus Lapis version/About metadata. Their Autodocs embeds MUST use the shared isolated 700px padding-free shell viewport rather than inheriting Storybook's default preview height, and authored MDX MUST render each scenario's canonical story description.                                                                                                                      |
| LN-CAT-014 | Workspace shell stories MUST cover the persisted desktop and mobile shells, the populated notification center, the Lapis About dialog, and stacked empty tabs whose scroll width and selected-tab movement match design-core.                                                                                                                                                                                                                                                                                                                                                                                             |
| LN-CAT-015 | `Workspace/Shell` MUST include a focused bottom-panel/settings story that boots a real api `App`, persists panel geometry through the api writer, and proves design-core's built-in settings update shell alignment and ribbon presentation without loading plugins or adding settings persistence. The play MUST open Settings from the left-ribbon bottom control.                                                                                                                                                                                                                                                      |
| LN-CAT-016 | `Workspace/Lapis Editor Demo` MUST boot a real api `App` and required Storybook-local core plugins over the public memory vault, use one canonical seed, and cover runnable, same-file sync, Markdown frontmatter, Explorer mutation, editor settings, startup, failure, and opening-vault states. Its full-shell Autodocs embeds MUST use the same shared isolated 700px padding-free viewport as `Workspace/Shell` and panel shell Docs, and authored MDX MUST render each scenario's canonical story description.                                                                                                      |
| LN-CAT-017 | Workspace catalog metadata MUST enumerate each editor-demo scenario, map it to this specification, and retain nested-import `visual-pending` coverage until human approval.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| LN-CAT-018 | Storybook MUST resolve every design-core workspace entry point and `@lapismd/storybook-addon-visual-delta` through their installed sibling links and public exports. Docker visual capture MUST exercise those same linked packages from ignored staged copies, avoiding a mixed installed/source runtime without consumer-owned source aliases.                                                                                                                                                                                                                                                                          |
| LN-CAT-019 | Storybook's Vite aliases MUST resolve `@lapis-notes/api/editor` with the root API source so editor interactions and accessibility checks exercise the protected implementation under test.                                                                                                                                                                                                                                                                                                                                                                                                                                |
| LN-CAT-020 | Storybook MUST resolve `@lapis-notes/markdown` from Lapis workspace source and linked Mira through built public exports, without Mira source aliases. It MUST provide focused `Plugins/Markdown/Panels/*` stories for all six Markdown command panels and prebundle dependencies introduced by governed panel interactions.                                                                                                                                                                                                                                                                                               |
| LN-CAT-021 | Storybook MUST expose each non-summary chapter indexed by `spec/src/SUMMARY.md` as a first-positioned `Specification/*` documentation page in summary order. Nested mdBook chapters MUST retain the same hierarchy in the Storybook sidebar, including Workspace Shell / Panels and each Markdown Plugin / Panels page. Each page MUST render its canonical `spec/src` Markdown through a metadata-only MDX adapter using a raw import and Storybook's `Markdown` block; normative prose MUST NOT be copied into Storybook-owned files.                                                                                   |
| LN-CAT-022 | `Plugins/Markdown/Panels/All Properties` MUST group six focused child stories for Middle (Top Tabs), Stacked Tabs, Left Sidebar, Right Sidebar, Bottom Panel, and Sidebar As A Group.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| LN-CAT-023 | File Properties, Outline, Backlinks, Outgoing Links, and Tags MUST each follow the `LN-CAT-022` six-surface pattern in their own nested group.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| LN-CAT-024 | Every Storybook Docs Show Code panel MUST present copy-pasteable usage of the real documented component boundary. Public components MUST use public package imports and include the props, state, composition, or persisted layout needed to reproduce the scenario. Show Code MUST NOT present a story-only demo component, harness, decorator, fixture selector, or inferred story args as the consumer API.                                                                                                                                                                                                            |
| LN-CAT-025 | A story whose render path differs from consumer usage MUST define explicit `parameters.docs.source` with `code`, `language`, and `type: "code"`. Nontrivial source SHOULD be colocated in `*.example-sources.ts` and MUST derive from the same canonical fixture or layout builder as the story.                                                                                                                                                                                                                                                                                                                          |
| LN-CAT-026 | An intentionally Storybook-local component MUST use its honest co-located fixture import in Show Code and identify that package-boundary exception. Documentation MUST NOT invent a public package export for a story-only component.                                                                                                                                                                                                                                                                                                                                                                                     |
| LN-CAT-027 | All Properties stories MUST declare the real exported `AllProperties` component as Autodocs authority and expose only `app: App`. Harness-only kind and layout selection MUST remain outside args, Controls, and Properties.                                                                                                                                                                                                                                                                                                                                                                                              |
| LN-CAT-028 | All Properties stories MUST use the smallest real persisted Lapis shell that keeps one panel instance as the visual and interaction subject.                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| LN-CAT-029 | Each All Properties play MUST assert the stable destination host, nested `WorkspaceViewHost`, and resolved paint inherited by the panel and sticky chrome.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| LN-CAT-030 | App-backed panel Autodocs MUST use isolated 700px iframes and remove Storybook story padding so each shell owns its preview and application state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| LN-CAT-031 | File-scoped panel stories MUST retain one minimal active Markdown leaf. Document-independent Tags stories MUST remain document-free.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| LN-CAT-032 | Each specialist-panel play MUST verify one panel, its placement host and paint, real grouped chrome where applicable, its documented source boundary, and its defining interaction.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| LN-CAT-033 | Direct Lucide entry points used by governed panel stories MUST be included in Storybook dependency optimization so clean browser tests do not reload during rendering.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| LN-CAT-034 | The retired flat dual-panel comparison stories MUST NOT remain in the Storybook catalog.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| LN-CAT-035 | Movable-panel stories MUST retain literal `visual-pending` tags and independent nested-import baseline paths until human review. Baseline capture MAY remain deferred when visual testing is explicitly excluded.                                                                                                                                                                                                                                                                                                                                                                                                         |
| LN-CAT-036 | Bottom Panel and Sidebar As A Group stories MUST use the real grouped-panel and sidebar-group chrome.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| LN-CAT-037 | Production Markdown panel Autodocs, including Tags, MUST use their public `@lapis-notes/markdown` app-only components.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| LN-CAT-038 | The Search panel catalog MUST expose consumer-usable source importing `@lapis-notes/search`, retain independent pending visual paths for six placements, and exercise its real indexed App.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| LN-CAT-049 | `Plugins/Bases/Views` MUST cover table, editable cells with text- and chrome-targeted edit mode, blur-dismissed autocomplete, a continuous active boundary, centered 1rem checkboxes, white on-accent glyphs, wrapped rows, a full-height shadcn Scroll Area without bottom or inline-end container frames but with a final-row separator, anchored filter selects, aligned value editors, highlighted expressions, filter width and leading right/down disclosure indicators, live resize and scroll alignment, cards, grouped list, map-unavailable, and unknown layouts.                                               |
| LN-CAT-050 | `Plugins/Bases/Shell` MUST boot a real App with File Explorer, retained collapsed Search, and an opened Bases file over the canonical seed. Bases file-view, Markdown-embed, and disable/restore scenarios MUST live beneath `Plugins/Bases`, verifying associations, source/preview switching, persistence, reopen, invalid YAML, cleanup, managed settings, and missing-view recovery.                                                                                                                                                                                                                                  |
| LN-CAT-051 | Bases stories MUST share one canonical seeded vault fixture, use `BasesViewSurface` as focused Autodocs authority, hide harness state from Controls, and provide explicit public-package Show Code. App stories MUST use isolated 700px iframes; every story remains `visual-pending` with an independent nested-import baseline until review.                                                                                                                                                                                                                                                                            |
| LN-CAT-055 | `Plugins/AI/Chat` MUST demonstrate the public `@lapis-notes/ai` chat panel with Fake runtime, including send/complete plus pending permission and agent-question Composer Drawer paths, explicit Show Code, and `visual-pending` nested-import baselines.                                                                                                                                                                                                                                                                                                                                                                 |
| LN-CAT-059 | `Plugins/AI/Chat` MUST demonstrate vault-scoped `@` file mentions through the public composer trigger, using Fake runtime, public Show Code, and a `visual-pending` nested-import baseline.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| LN-CAT-061 | `Plugins/AI/Shell` MUST boot a real App, fill the standalone viewport while the bottom panel is closed, keep Explorer visible on the left and Search retained collapsed on the right, and demonstrate the server-backed model selector without free text. Its empty transcript MUST fill the remaining panel above the bottom composer.                                                                                                                                                                                                                                                                                   |
| LN-CAT-062 | `Plugins/AI/Chat` MUST demonstrate a Fake rich turn with visible thinking, expandable tool input and output, Markdown assistant text, a date divider, timestamps, a Copy response action, Design Core Composer Drawer attachment chips, a streamed context-usage bar, filtered provider bookkeeping statuses, an active-turn working indicator, and checked Model and Thinking submenus. Deterministic failure scenarios MUST cover both composer top validation and a failed assistant message with Retry. It MUST use public Show Code and retain a `visual-pending` nested-import baseline.                            |
| LN-CAT-056 | `Plugins/Bases/Workflows` MUST cover query controls, schema and view settings, and persisted typed metadata editing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| LN-CAT-057 | The focused Bases Editable Cells story MUST hit-test the AND/OR and operator Select portals above the enclosing filter panel and MUST prove the first Add Filter activation increments the visible predicate count.                                                                                                                                                                                                                                                                                                                                                                                                       |
| LN-CAT-058 | The focused Bases Editable Cells story MUST hit-test the filter panel above each intersecting sticky table header before exercising nested filter menus.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| LN-CAT-060 | The focused Bases Editable Cells story MUST open Sort and Filter in the same viewport and assert their computed outer widths match within sub-pixel tolerance.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| LN-CAT-063 | The focused Bases Editable Cells and real editor-shell stories MUST enter advanced-filter edit mode, open CodeMirror completions, and assert the inline editor has no default outline while the completion surface has opaque, rounded, shadowed Lapis popover chrome.                                                                                                                                                                                                                                                                                                                                                    |
| LN-CAT-064 | The real Bases editor-shell story MUST retain Search while starting and finishing with the right sidebar collapsed. Its interaction MUST open the sidebar between those assertions and verify the indexed query and result before restoring the collapsed final state.                                                                                                                                                                                                                                                                                                                                                    |
| LN-CAT-065 | `Plugins/AI/Chat` MUST demonstrate Layout scroll-to-latest after the transcript is scrolled away from the newest item, using a seeded Fake session, public Show Code, and a `visual-pending` nested-import baseline.                                                                                                                                                                                                                                                                                                                                                                                                      |
| LN-CAT-066 | Storybook Vite MUST resolve `@lapis-notes/ai/runtimes` to the plugin source adapter entry.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| LN-CAT-067 | Default `Plugins/AI` stories MUST stay Fake. Storybook MUST NOT start `lapis-ai-host`. Preview MAY register a live bridge from sibling `@lapismd/ai-host/client` using a configured URL and token, and MUST resolve that package through its installed link rather than a source alias.                                                                                                                                                                                                                                                                                                                                   |
| LN-CAT-068 | `Plugins/AI/Live Host` MUST be the manual live lane, tagged `skip-visual`, with no visual baseline. ManualAttach MUST cover missing-attach setup and MUST NOT send a live prompt. ReloadResume MUST seed the shared `lapis-ai-story:` key and assert transcript, divider, and usage before resume without sending a prompt.                                                                                                                                                                                                                                                                                               |
| LN-CAT-071 | `Plugins/AI/Shell` MUST include deterministic Local Conversations, Agent Switching, and Recovery scenarios. Agent Switching MUST reconstruct Codex to Cursor to Codex from mixed portable records, assert full then delta audit fields, show the effective in-place model change, and prove typed handoff content did not enter `transcript.jsonl`. `Plugins/AI/Panels/History` MUST cover the Explorer-style tree, active scope, archived option, highlighted search, centered search chrome, placement-neutral paint, public Show Code, and all six governed placements.                                                |
| LN-CAT-092 | `Plugins/AI/Panels/History` plays MUST assert the dimmed creation-folder path, Design Core tooltips on chrome actions, that selecting a History tree folder updates that path, and that folder counts share one trailing edge.                                                                                                                                                                                                                                                                                                                                                                                            |
| LN-CAT-093 | `Plugins/Graph/Panels/Graph` and `Plugins/Graph/Panels/Local Graph` MUST each render the registered App-backed view in middle-tab, stacked-middle, left, right, bottom-group, and sidebar-group placements. They MUST use public `@lapis-notes/graph` Show Code, isolated 700px Docs, deterministic indexed fixtures, independent visual paths, and `visual-pending`. Plays MUST assert disclosure headers, muted idle paint, explicit active-file focus, immediate labels, delayed emphasis, and scoped hover-label paint.                                                                                               |
| LN-CAT-094 | Focused Graph plays MUST exercise shared filter and Group autocomplete through real portals, compact add/reorder/recolour/diagnose/remove behavior within the viewport-clamped 300px panel, including Design Core colour presets and unrestricted native picking plus right-padding remove controls that do not overlap query or colour controls, both hover-delay controls, modifier Markdown preview positioning and pointer handoff, Animate/Stop, governed force and thickness ranges, and all seven semantic colour roles. They MUST assert interaction and hit-testing independently from visual-baseline approval. |
| LN-CAT-095 | Focused Graph plays MUST assert balanced handle spacing, divider- and shadow-free idle rows, the absence of a handle mouse-hover outline in the active browser stylesheet, rounded whole-row focus paint covering every control while retaining an outer trailing gap, a visible and hit-testable close action with a trailing gutter and token-driven hover paint, and non-overlapping query, colour, and right-padding close geometry. Changed visuals MUST remain `visual-pending`.                                                                                                                                    |
| LN-CAT-072 | The Live Host missing-attach state MUST point to a seeded supervisor that starts an isolated loopback host and injects an ephemeral token into Storybook without writing or logging it. Storybook itself MUST NOT start the host, and lower-level manual attachment MUST remain available.                                                                                                                                                                                                                                                                                                                                |
| LN-CAT-073 | `Plugins/AI/Shell` MUST use a story-controlled initialization gate to assert immediate disabled chat chrome followed by an enabled composer without timing assumptions. Its local-conversations play MUST assert that History preserves the chat leaf and selecting the same conversation twice reuses one main-area tab.                                                                                                                                                                                                                                                                                                 |
| LN-CAT-074 | Story titles and physical catalog paths for bundled plugins MUST use `Plugins/<Plugin>`. Command-access panels MUST use `Plugins/<Plugin>/Panels/<Panel>` and full app examples MUST use `Plugins/<Plugin>/Shell`; `Workspace/Panels` and `Workspace/Plugins` are forbidden. The nested `Specification` group MUST be the first top-level Storybook menu item and preserve SUMMARY order.                                                                                                                                                                                                                                 |
| LN-CAT-075 | Explorer, Bookmarks, Search, History, AI Chat, AI History, AI Catalog, and the six Markdown command panels MUST each demonstrate Middle Top Tabs, Stacked Tabs, Left Sidebar, Right Sidebar, Bottom Panel, and Sidebar As a Group through real workspace chrome. Standalone previews MUST fill the viewport with no canvas padding or document scrolling; only Docs canvases use the isolated 700px height.                                                                                                                                                                                                               |
| LN-CAT-076 | Every plugin Shell family MUST provide Desktop and Mobile stories with Explorer visible on the left and Search retained on the collapsed right. Standalone shells MUST fill the viewport without canvas padding or page scrolling while only Docs canvases use the isolated 700px height. Plays MAY reveal Search but MUST collapse it before completion.                                                                                                                                                                                                                                                                 |
| LN-CAT-077 | Workspace Shell PersistedDesktop and Mobile MUST load Markdown, Markdown Lint, Explorer, Bookmarks, Search, History, Bases, AI, and Word Count as enabled bundled plugins. The AI runtime MUST remain deterministic Fake. Those stories MUST assert F-Mode is present and disabled.                                                                                                                                                                                                                                                                                                                                       |
| LN-CAT-078 | Lapis Storybook MUST NOT contain Roles or CV stories, titles, fixtures, aliases, or Storybook-only imports. Roles/CV catalog acceptance belongs to `lapis-plugin-cv-roles`; production host registration remains unchanged.                                                                                                                                                                                                                                                                                                                                                                                               |
| LN-CAT-086 | Lapis Storybook MUST NOT contain Terminal stories, titles, fixtures, aliases, or Storybook-only imports. Terminal catalog acceptance belongs to `lapis-plugin-terminal`; production host registration remains unchanged.                                                                                                                                                                                                                                                                                                                                                                                                  |
| LN-CAT-079 | AI Chat MUST provide independent deterministic stories for requested and accepted permissions, asked and answered questions, running/succeeded/failed tools, socket validation, failed response retry, and a rich response with thinking, usage, copy, attachments, timestamps, and filtered provider bookkeeping. Each state MUST retain public Show Code, interaction coverage, and an unapproved visual path.                                                                                                                                                                                                          |
| LN-CAT-080 | Every SUMMARY chapter MUST have one exact metadata-only Storybook mirror and Storybook configuration MUST include that mirror path. A post-build check MUST compare `storybook-static/index.json` with SUMMARY and fail when any specification chapter did not render.                                                                                                                                                                                                                                                                                                                                                    |
| LN-CAT-081 | AI Chat MUST provide deterministic application-tool stories for a read result, pending write diff, allow-once, allow-for-binding reuse, denial, community-plugin enablement, and an unavailable host capability. Each changed visual MUST remain `visual-pending` until human baseline review.                                                                                                                                                                                                                                                                                                                            |
| LN-CAT-082 | Storybook Vite MUST resolve the first-party File Explorer and Search package roots, plus portable API subpaths imported by first-party plugins, from their Lapis workspace source. Canonical Docker capture MUST therefore remain independent of package `dist` output excluded from its staged workspace while public examples continue to document package imports.                                                                                                                                                                                                                                                     |
| LN-CAT-083 | `Plugins/AI/Shell` Desktop MUST expose the plugin's Open Chat left-ribbon action and prove activating it reuses the existing main-area chat leaf.                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| LN-CAT-084 | `Workspace/Shell` PersistedDesktop MUST show File Explorer, Search, then Bookmarks on the left and Outline, File Properties, then Tags on the right after bundled plugins load. The Bookmarks left-sidebar play MUST cover toolbar actions, typed rows, filter, add/remove group refresh, row inset, toolbar hover, Explorer tree geometry, leaf indent, drag reparent, search activation, and graph persistence.                                                                                                                                                                                                         |
| LN-CAT-085 | `Workspace/Shell` PersistedDesktop MUST keep Design Core tooltip paint inverted in light mode after bundled Markdown loads Mira styles. Hovering File Explorer chrome MUST show a dark surface and light text.                                                                                                                                                                                                                                                                                                                                                                                                            |
| LN-CAT-087 | `Plugins/AI/Chat` MUST demonstrate a Fake folder skill, reserved `/help` grouping, and a reserved composer slash command through the public chat panel. The play MUST use public Show Code and MUST NOT require a live agent.                                                                                                                                                                                                                                                                                                                                                                                             |
| LN-CAT-088 | `Plugins/AI/Chat` MUST demonstrate extension `/open-daily-note` and that disposing the registration removes it. The play MUST use public Show Code and MUST NOT require a live agent.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| LN-CAT-089 | `Plugins/AI/Panels/Catalog` MUST cover the nested owner and kind tree, a row-click tool description expand, expand-all, a filter, a persisted tool checkbox, Open on a vault `SKILL.md`, Open on a vault command Markdown file, public Show Code, and all six governed placements.                                                                                                                                                                                                                                                                                                                                        |
| LN-CAT-090 | `Plugins/AI/Chat` MUST demonstrate a completed `notes_search` tool item with Search-owned hit rows. A hit click MUST open that vault file while the chat panel remains. The play MUST use public Show Code and MUST NOT require a live agent.                                                                                                                                                                                                                                                                                                                                                                             |
| LN-CAT-091 | `Plugins/AI/Shell` MUST demonstrate two descendant conversations in a Command View; use the transcript scope control to select Vault root and assert Explorer follows; open a stored folder conversation and assert both chat and Explorer scope; then pin it and keep that transcript after the active file changes. The play MUST use public Show Code and MUST NOT require a live agent.                                                                                                                                                                                                                               |
| LN-CAT-069 | App-backed stories MUST install disposable compatibility leases while their canvases use the App they own. Multi-App acceptance MUST prove Workspace, Bases, and AI do not follow another story's compatibility alias.                                                                                                                                                                                                                                                                                                                                                                                                    |
| LN-CAT-070 | The Storybook browser project MUST retain a bounded 120-second per-story ceiling so real-App database and linked-plugin scenarios remain reliable under full-suite parallel load. Scenario-local waits MUST remain narrower and focused runs MUST still diagnose functional failures independently.                                                                                                                                                                                                                                                                                                                       |

The governed Tags, All Properties, Backlinks, Outgoing Links, Search, Graph,
Local Graph, and Bases
families start from persisted indexed data and include visible async-query
failure/recovery acceptance. Their changed stories remain `visual-pending` and
capture may produce review artifacts, but this implementation does not approve
new baselines.

The application-tool story family uses deterministic transcript seeds and an
inert resumable runtime to keep approval cards pending without starting an
agent. Seeded read and write cards use the OpenClaw-named `read` and `edit`
tools. The community-tool shell story mutates the real AI settings contract and
verifies Search, Markdown `notes_list`, and API file-tool enablement plus the
disabled and newly enabled community-tool states.

The Specification sidebar follows SUMMARY order, including Deno Desktop Host
and Deno Desktop Parity Blockers immediately after Desktop Host. Those pages
are metadata-only raw-import mirrors.

## Show Code source contract

Production desktop and web hosts continue to register external Roles in their
owned runtime order, but Lapis Storybook does not import that package. Focused
Bases stories remain responsible for the governed public-surface and workflow
scenarios rather than copying package source.
The focused Bases view family boots that optional bundled plugin and shares one
Projects seed across table, editable-cell, cards, grouped list, map fallback,
and unknown-view scenarios. Its editable table verifies opaque controls,
centered 1rem checkboxes, white on-accent glyphs, wrapped tag and multivalue rows,
metadata suggestions, a full-height bottom-edge shadcn Scroll Area, legacy
filter-popover geometry, start-position disclosure indicators, and aligned row
and column geometry through live resizing and scrolling.
The workflow family exercises query, filter, sort, grouping, limit, schema,
formula, summary, visibility, view-management, layout, typed-editing, new-file,
CSV, and every public custom-option contract. The real-App family exercises
the file association in source and preview, read-only Markdown embeds, invalid
YAML cleanup, and managed disable/restore without mutating the Bases document.

Show Code documents consumer integration, not Storybook implementation:

- Import a public component from its package boundary.
- Include the real props, state, composition, or persisted layout that produces
  the scenario.
- Replace automatic extraction with explicit `parameters.docs.source` when the
  story renders through a demo component, harness, decorator, or fixture.
- Keep nontrivial example strings beside the story and derive them from the same
  canonical data or layout builder.
- For an intentional Storybook-local component, show its real co-located import
  and state why no public package import exists.

`pnpm spec:validate` statically checks repository and authorized package-local
Autodocs stories that render through local demo, harness, or fixture
boundaries. Those stories must resolve an explicit source object with code,
language, and fixed `type: "code"` fields; acceptance-only stories tagged
`!autodocs` are outside this documentation gate.

## API verification families

Direct api imports (one story family each):

- `button`, `input`, `textarea`, `switch`, `slider`, `progress`
- `select`, `search`, `tooltip`, `popover`, `command`
- `dropdown-menu`, `context-menu`, `drawer`
- `modal`, `confirm-dialog`, `date-setting` (`@lapismd/design-core/forms` DatePicker/TimePicker)
- `scroll-area`, `table`, `toggle-group`
- `sidebar-custom`, `table-dnd`
- `helpers` (`cn` / `fuzzySearch`) — interaction-only, `skip-visual`

The `Plugins/AI/Chat` attach play asserts a Command View host inside the
paperclip popover, visible border and shadow, and stacking above an open
composer drawer.
The same Fake chat family includes a `skip-visual` automatic-memory-recall
scenario. Its play enables recall, submits one turn, and proves a typed recall
block reached the Fake session exactly once while the visible and durable user
prompt remains authored text only. Memory maintenance and review surfaces stay
unit- and command-covered until their visual review UI is promoted to a
governed catalog family.
The `Plugins/AI/Shell` Local Conversations play opens the composer overflow
menu, asserts Archive Chat, Delete Chat, and New Chat, checks those labels
are not clipped, and matches their font size to the model menu (LN-AI-109,
LN-AI-123).

The structured plugin-panel catalog covers Explorer, Search, History, AI Chat, AI
History, AI Catalog, and the six Markdown command views. History compare stories assert
`History` plus parent-path header breadcrumbs and that History opens or reveals
the History panel. `Plugins/History/Shell` provides Desktop and Mobile full-app
stories with Explorer on the left, a multi-section Welcome compare in the
main area, and Search retained on the collapsed right. The Desktop play
opens Settings and proves the History exclude/include configuration. Every mapped story renders the
same registered view once in Middle Top Tabs, Stacked Tabs, both sidebars, a
real bottom-panel group, and a real sidebar group; new or changed captures stay
`visual-pending` until reviewed.

## Tooling

- Interaction + a11y tests: `pnpm test:storybook` (axe via addon-a11y; `a11y.test: "error"`)
- No-Tailwind sources: `pnpm check:no-tailwind` (component trees; stories excluded)
- Visual baselines: `pnpm build-storybook` then `pnpm test:visual:update` (generates nested-import baselines under `tests/visual/storybook.spec.ts-snapshots/stories/`)
- Visual regression: `pnpm test:visual`
- Workspace shell stories use the same interaction, a11y, and nested-import
  Visual Delta workflow as API stories and remain `visual-pending` until review.
- Editor-demo catalog entries identify the Ready, MarkdownProblems,
  MarkdownLintLoftBoarding, SameFileSplitSync, MarkdownFrontmatter,
  ExplorerMutations, EditorSettings, LoadingPlugins, MarkdownAuthoring,
  StartupFailure, and ExplorerOpeningVault stories individually so focused
  visual updates do not rewrite existing shell baselines. The loft-boarding
  Markdownlint story keeps literal `visual-pending` metadata without a
  captured baseline. MarkdownAuthoring uses the real in-memory app and editor stack,
  consumer-oriented Show Code, literal `visual-pending` metadata, and a future
  baseline path while its pointer test performs real block dragging.
- The bottom-panel/settings story keeps the Workspace settings page open in its
  final state so panel alignment and controller-backed settings are visible.
- Shell story views remain API-registered imperative Lapis views; their
  story-only body title responds to the live inline-title appearance setting so
  settings are demonstrated through the same view-host bridge as consumers.
- The visual helpers stage the sibling design-core and Visual Delta checkouts
  inside the Docker context for both compare and update runs. They exclude
  documented nonvisual stories, and the update helper accepts repeatable
  story-prefix filters; its defaults select API and workspace stories without
  rewriting unrelated baselines.
  App rebuild-vault-cache and rebuild-generated-state, plus web vault copy
  progress, stay on the notifications or startup-detail surfaces and do not add
  catalog stories (LN-PKG-097, LN-PKG-098, LN-WEB-042, LN-WEB-043).
