import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./LapisEditorDemo.stories";

<Meta of={Stories} />

# Lapis source editor demo

This additive demo boots a real API `App` over the public in-memory vault,
loads required story-local core plugins, restores the design-core workspace,
and edits Markdown, text, and JSON files in source mode.

<Canvas of={Stories.Ready} />

<Canvas of={Stories.SameFileSplitSync} />

<Canvas of={Stories.MarkdownFrontmatter} />

<Canvas of={Stories.ExplorerMutations} />

<Canvas of={Stories.EditorSettings} />

<Canvas of={Stories.LoadingPlugins} />

<Canvas of={Stories.StartupFailure} />

<Canvas of={Stories.ExplorerOpeningVault} />
