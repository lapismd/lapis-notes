import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./Select.stories";

<Meta of={Stories} />

# Select

API verification for `@lapis-notes/ui/select`. See `spec/src/storybook-catalog.md`.

<Canvas of={Stories.Select} />
