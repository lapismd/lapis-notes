import { Meta, Canvas } from "@storybook/addon-docs/blocks";
import * as Stories from "./Helpers.stories";

<Meta of={Stories} />

# Helpers

API verification for `@lapis-notes/ui` helpers (`fuzzySearch`). Tagged `skip-visual`. See `spec/src/storybook-catalog.md`.

<Canvas of={Stories.Helpers} />
