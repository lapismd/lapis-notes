---
description: Commit verified changes in this Lapis Notes package with Jujutsu without waiting to be asked
globs: packages/api/**
alwaysApply: false
---

# Commit after every verified change

This package follows the repository standing commit request. After a
validated slice here, commit with the owning `lapis-notes` `AGENTS.md` and
the `jj-jujutsu` skill. Do not wait for a later "commit" message.

Path-scope the commit when unrelated files are dirty. Leave secrets, loader
symlinks, and generated `spec/book/` output uncommitted.
